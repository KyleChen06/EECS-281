{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug",
      "program": "${workspaceFolder}/project0_debug",
      "args": ["-h", "--mode", "nosize", "resize", "reserve"],
      "stdio": ["sample-r.txt", "output.txt", null],
      "cwd": "${workspaceFolder}",
      "env": {
          "ASAN_OPTIONS": "abort_on_error=1:detect_leaks=0"
      }
    }
  ]
}